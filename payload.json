{
  "model": "gpt-oss-120b-nim",
  "input": [{ "role": "user", "content": "What time is it in Amsterdam? Use the tool." }],
  "tools": [{
    "type": "function",
    "name": "get_time",
    "description": "Get local time",
    "parameters": { "type": "object", "properties": { "city": { "type": "string" } }, "required": ["city"] }
  }],
  "tool_choice": "auto"
}
